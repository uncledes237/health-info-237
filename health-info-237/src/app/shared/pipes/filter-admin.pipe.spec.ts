import { FilterAdminPipe } from './filter-admin.pipe';

describe('FilterAdminPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAdminPipe();
    expect(pipe).toBeTruthy();
  });
});
