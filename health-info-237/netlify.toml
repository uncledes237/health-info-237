[build]
  base = "health-info-237"
  command = "npm install --legacy-peer-deps && ng build --configuration production"
  publish = "www"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 